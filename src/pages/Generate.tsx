import PageLayout from "../layout/PageLayout";

const Generate = () => {
  return (
    <PageLayout>
      <div className="grid">
        <div>Generate</div>
      </div>
    </PageLayout>
  );
};

export default Generate;
